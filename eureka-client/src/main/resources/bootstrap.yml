server:
  port: 8890
eureka:
  client:
    serviceUrl:
      defaultZone:    http://127.0.0.1:8000/eureka/
spring:
   application:
     name: asgard-test-service
logging:
  level:
    com.netflix.discovery: debug
    me.flyleft.eureka.client.event.AbstractEurekaEventObserver: info
