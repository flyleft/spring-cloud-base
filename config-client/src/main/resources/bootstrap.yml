server:
    port: 8770
eureka:
    client:
        serviceUrl:
            defaultZone:    http://127.0.0.1:8000/eureka/
spring:
  application:
    name: config-client
  cloud:
    config:
      profile: test
      label: master
      uri: http://localhost:8769/