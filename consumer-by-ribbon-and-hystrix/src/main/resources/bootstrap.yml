server:
    port: 8772
eureka:
    client:
        serviceUrl:
            defaultZone:    http://127.0.0.1:8000/eureka/
spring:
    application:
        name:   consumer-ribbon-hystrix
ribbon:
  eager-load:
    enabled: true
    clients: client-server-a