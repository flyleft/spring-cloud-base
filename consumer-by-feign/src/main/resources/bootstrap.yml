server:
  port: 8768
  tomcat:
      max-threads: 1100
      max-connections: 1100
eureka:
  client:
    serviceUrl:
      defaultZone:    http://127.0.0.1:8000/eureka/
spring:
   application:
     name:  consumer-feign-service
   datasource:
     url: jdbc:mysql://localhost/hap_event_service?useUnicode=true&characterEncoding=utf-8&useSSL=false
     username: hapcloud
     password: handhand